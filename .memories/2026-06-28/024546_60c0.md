---
id: checkpoint_60c0fd90
timestamp: 2026-06-28T02:45:46.880Z
tags:
  - url-import
  - open-brain
  - recipe-run
  - paywall
  - bot-block
  - dotdash-meredith
  - queue-cleanup
git:
  branch: kae36/personal
  commit: 8fbc984
  files:
    - .gitignore
    - 2026-05-05 22_49_09-Clipboard.png
    - recipes/url-batch-import/.miller/indexer.lock
    - recipes/url-batch-import/.miller/symbols.db
    - recipes/url-batch-import/.miller/symbols.db-shm
    - recipes/url-batch-import/.miller/symbols.db-wal
summary: URL Batch Import — 2nd run (+45 imported) and Articles.txt pruned to
  failure-only queue
briefId: url-batch-import-recipe-quick-start
type: checkpoint
next: "The 14 queued failures need option #2 (headless browser / browser39 +
  logged-in session) to crack — the Dotdash Meredith 403 cluster especially.
  food52 (×2, HTTP 429) may succeed on a plain re-run once rate-limit resets.
  Cheap immediate retry possible anytime; persistent 403s won't change without
  the browser approach."
confidence: 5
---

## URL Batch Import — 2nd run (+45 imported) and Articles.txt pruned to failure-only queue

### Run result
Re-ran `import-urls.ts` on the user-trimmed Articles.txt (user deletes already-imported lines manually, so file was down to 111 URLs). Result: **45 newly ingested**, 52 already-synced (skipped), **14 fetch-failed**. Big jump from the prior run's 1 — because the file now holds fresh recent articles, most of which are normal news/lifestyle sites that fetch fine (NPR, AP, Vox, GQ, Slate, Time, New Yorker, NYT, arstechnica, ProPublica, Smithsonian, BBC, etc.).

### The 14 failures (status codes from failures.log)
- **403 bot-block:** axios + a **Dotdash Meredith / People Inc. cluster** — marthastewart ×3, bhg, southernliving, realsimple, allrecipes, thespruce, travelandleisure. These share bot-blocking infra and fail as a group, so one fix (headless browser) unlocks all.
- **429 rate-limit (transient):** food52 ×2 — worth a later retry.
- **http2 stream error:** washingtonpost ×2.

### Articles.txt pruned to a clean queue
Removed all 97 imported URLs (45 new + 52 lingering already-synced) by filtering against `sync-log.json`'s `ingested_urls` (stores full URLs verbatim incl. utm suffix, so exact `grep -vxFf` match). File went 111 → **14 URLs** = exactly the unreachable ones. Backup: `Articles.txt.bak-20260627-224513` in the Dropbox folder. Confirmed sync-log keys match file URLs verbatim.

### Notes
- Nothing to git-commit from the run itself — `sync-log.json`/`failures.log` are gitignored, machine-local. Articles.txt lives in Dropbox (syncs separately).
- Running total in brain: sync-log now tracks 696 ingested URLs.
