---
id: checkpoint_fe7c53a2
timestamp: 2026-09-19T05:06:34.962Z
tags:
  - gitignore
  - git-exclude
  - upstream-sync
  - housekeeping
git:
  branch: kae36/personal
  commit: 988e3cb
  files:
    - .gitignore
actor:
  harness: claude-code
  user: kae
  git_user: Alan Edwards
  git_email: kaeatxnet@gmail.com
summary: Moved machine-local ignores out of tracked .gitignore into .git/info/exclude
briefId: bring-my-open-brain-up-to-date
type: decision
decision: Keep machine-local ignore entries in .git/info/exclude and keep the
  fork's tracked .gitignore identical to upstream.
alternatives:
  - "Commit /ScratchPad and .code-kb/ to the tracked .gitignore: simpler and
    syncs across clones, but adds conflict risk on each upstream sync."
next: Pick the next capability to adopt (check the landmine catalog first);
  url-batch-import branch housekeeping still outstanding.
---

## Moved machine-local ignores out of tracked .gitignore into .git/info/exclude

**WHAT**
- `.git/info/exclude` now lists `/ScratchPad`, `/.code-kb/`, `.claude`, `.codex`, `.julie`, `.julieignore`, `.miller`.
- `.gitignore` restored to match `origin/main` exactly. That removed the 5 local-only lines already committed on `kae36/personal` (`.claude`, `.codex`, `.julie`, `.julieignore`, `.miller`) and the uncommitted `/ScratchPad` line.

**WHY**
- Resolves the open item from the 2026-09-13 brief (the `/ScratchPad` gitignore question deferred after the first upstream merge).
- `.gitignore` is tracked and edited by upstream, so local-only lines there add merge-conflict risk on every upstream sync and diff noise on any PR from this fork.

**HOW**
- Verified with `git check-ignore -v` that all seven entries are still ignored, including new files under `.claude/`.
- `.claude/skills/review-pr.md` is tracked (from upstream) and was already tracked before this change, so nothing changes for it.
- `.code-kb/` no longer shows as untracked.

**IMPACT**
- The next upstream sync should not conflict on `.gitignore`.
- `.git/info/exclude` is per clone and does not sync. Any other clone (e.g. the KVM guest) needs the same seven lines. Worktrees created from this repo inherit the main `.git/info/exclude`.
- `.planning/` stays ignored through upstream's own `.gitignore` entry. `.memories/` remains tracked.
