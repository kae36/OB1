---
id: checkpoint_49ed9334
timestamp: 2026-08-05T04:06:15.814Z
tags:
  - security
  - supabase
  - session-close
  - brief-update
  - merged
git:
  branch: kae36/personal
  commit: ed47daf
summary: Session close — security remediation merged, brief updated
briefId: bring-my-open-brain-up-to-date
type: checkpoint
impact: Source and database are now consistent and least-privilege. Future
  capability adoption has an explicit security-verification step that would have
  caught this class of defect.
evidence:
  - "gh pr view 1: state MERGED, mergeCommit
    ed47dafb83b2b07438cfb74f5966d03ce2f8c662"
  - "Post-merge grep on kae36/personal: ops-views.sql 9 security_invoker hits,
    author-session-id.sql 2, crm-person-tiers 2 ENABLE ROW LEVEL SECURITY"
  - "harden-grants.sql final audit: 10 rows, all 'ok - all privileges are
    RLS-filtered', anon absent throughout"
next: Re-run Advisors -> Security in the Supabase dashboard to confirm clean -
  the only unverified step of the remediation. Then resume the capability queue
  (enhanced-mcp is the largest remaining lift).
confidence: 5
---

## Session close — security remediation merged, brief updated

Closing checkpoint for the 2026-08-04/05 Supabase security session.

### Merged

Fork PR #1 → `kae36/personal` as merge commit `ed47daf`. Verified on disk after pull: all 8 `ops_*` views and `thought_provenance` carry `security_invoker = true`, both CRM tables carry `ENABLE ROW LEVEL SECURITY`. This closes the regression path — re-running those schema files now reinforces the DB fix instead of undoing it.

Branch `contrib/kae36/fix-supabase-security-advisors` still exists locally and on the fork; content is preserved in the merge commit, safe to delete.

### Brief rewritten

`bring-my-open-brain-up-to-date` gained a full SECURITY section and three durable changes to how this project should be worked:

1. **Deploy playbook grew a step 5** — run the security audit (`policy-audit.sql` + `harden-grants.sql` in `.planning/supabase-security/`) before marking any table-creating capability DONE. Two already-DONE capabilities (`crm-person-tiers`, `brain-health-monitoring`) shipped the exposure precisely because the smoke test verified function, not access control.
2. **Dashboard SQL editor gotchas recorded** — returns only the last statement's result set, and a mid-script error rolls back the entire file.
3. **Environment note** — no local `psql` and no docker, so SQL cannot be validated before it runs against production.

### Deliberate non-actions, all user decisions

- No fork-only CI workflow (would diverge `.github/` and conflict on every upstream sync).
- Upstream `NateBJones-Projects/OB1` not notified, despite carrying the identical defect in four files.
- Duplicate policies on 7 tables left in place; `household_member` role existence unverified.

### Unrecoverable

Whether the exposure was exploited. API Gateway logs were never checked during the window; free-tier retention has likely expired. Flagged twice during the session, not actioned.
