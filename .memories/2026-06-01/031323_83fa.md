---
id: checkpoint_83fa4b9d
timestamp: 2026-06-01T03:13:23.169Z
tags:
  - recipe
  - url-import
  - open-brain
  - pr-339
  - dedup
  - ci
  - scoop
  - fingerprint
git:
  branch: contrib/kae36/url-batch-import
  commit: e5112f0
  files:
    - .gitignore
    - 2026-05-05 22_49_09-Clipboard.png
    - OPENROUTER_AUDIT.md
summary: "URL Batch Import recipe hardened + PR #339 opened (Open Brain / OB1)"
type: checkpoint
evidence:
  - "markdownlint-cli2: 0 errors"
  - 543/543 fingerprints backfilled, 0 collisions
  - idempotent re-import returns Duplicate
  - "PR #339 mergeable: MERGEABLE"
next: "Next session: check PR #339 (gh pr checks 339 --repo
  NateBJones-Projects/OB1). Confirm Markdown Lint went green after maintainer
  approval, address any claude-review/maintainer feedback, and rebase onto
  latest origin/main only when a maintainer is ready to merge."
---

## URL Batch Import recipe hardened + PR #339 opened (Open Brain / OB1)

Hardened the `url-batch-import` Open Brain recipe, cleaned up the database, and opened a contribution PR.

### Recipe fixes (`recipes/url-batch-import/import-urls.ts`)
- **Incremental sync-log persistence** — was only saved at the end of a run, so interrupted runs lost all dedup progress and re-downloaded/re-ingested on the next run. Now writes after every URL.
- **URL-stable fingerprint** — was hashing volatile content (today's date + non-deterministic LLM summary), so re-imports never deduped at the DB level. Now hashes a normalized URL (strips `utm_*`/tracking params, `www`, trailing slash, fragment).
- **`import.meta.main` guard** so the module is importable; added `backfill-compute.ts` helper that reuses the real `urlFingerprint`.

### Database cleanup (user's Open Brain / Supabase)
- Deleted **507 duplicate rows** (the user's Firefox bookmark list had been imported ~2x; old content-based fingerprints never caught it). Kept earliest of each; full backup saved to `~/openbrain-deleted-rows-backup-*.json`.
- **Backfilled 543 legacy rows** to URL-based fingerprints; verified idempotent re-import (legacy URL re-import → `Duplicate`).

### PR #339
- Branch `contrib/kae36/url-batch-import` on fork `kae36/OB1` → `NateBJones-Projects/OB1`. Recipe-only, 2 commits, secrets verified out (`.env` gitignored + recipe-local `.gitignore`).
- Proactively fixed 5 markdownlint MD031 errors (lint passes locally, 0 errors).

### CI reality
- `ob1-gate` startup-fails **repo-wide** (every PR + pushes to `main`) — a broken maintainer workflow, NOT our recipe.
- Markdown Lint pending first-time-contributor "Approve and run".
- PR is `MERGEABLE` but `BEHIND` (21 commits) — update branch only right before merge.

### Tooling (same session)
- Migrated `deno` choco→scoop, pruned user PATH to 19 entries (REG_EXPAND_SZ preserved), reclaimed 96 MB.
- Leonard Leo web research saved to Open Brain (9 thoughts).
