---
id: checkpoint_19a066b0
timestamp: 2026-07-04T05:40:25.295Z
tags:
  - decision
  - github
  - mcp
  - gh-cli
  - tooling
  - connector
  - auth
git:
  branch: kae36/personal
  commit: 0923a7a
summary: GitHub MCP connector broken; sticking with `gh`/`git` for now
briefId: url-batch-import-recipe-quick-start
type: decision
decision: Keep using gh/git for GitHub operations instead of troubleshooting the
  broken GitHub MCP connector
---

## GitHub MCP connector broken; sticking with `gh`/`git` for now

The GitHub MCP plugin failed to reconnect (`HTTP 400 at https://api.githubcopilot.com/mcp/`), likely an auth/token issue.

- **Discussion:** Evaluated whether fixing the connector is worthwhile. GitHub MCP's main draws (repo/code search via `search_repositories`, `search_code`, etc.) are already covered by `gh search repos` / `gh search code`.
- **Decision:** No action taken to fix the connector — `gh` + `git` remain sufficient for current GitHub workflows (PRs, issues, CI, search).
- **Next:** Revisit only if a specific gap surfaces that `gh` can't cover (e.g., org-wide security/Dependabot alerts, cross-repo search at scale, discussions).
